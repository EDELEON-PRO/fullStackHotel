import React from 'react';

const UserPanel = () => {
    return (
        <div>
            <h1>User Panel</h1>
            {/* Contenido exclusivo para usuarios */}
        </div>
    );
};

export default UserPanel;